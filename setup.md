
## Installation

```
cd ~/
git clone https://github.com/xsstnv/dotfiles
cd dotfiles
cat .bashrc >> ~/.bashrc
cp .bash_aliases ~/.bash_aliases
cp .bash_functions ~/.bash_functions
cat .terminator > ~/.config/terminator/config
```
